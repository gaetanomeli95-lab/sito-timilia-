import IntroPreview from "@/components/IntroPreview";

export const metadata = {
  title: "TIMILIA — Intro Preview",
  robots: { index: false, follow: false },
};

export default function IntroPreviewPage() {
  return <IntroPreview />;
}
